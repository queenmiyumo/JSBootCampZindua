const groupBy = [
     { name: 'Alice', age: '25', city:'New York'},
     { name: 'Bob', age: '30', city: 'Chicago' },
     { name: 'Charlie', age: '35', city: 'New York' },
     { name: 'Dave', age: '40', city: 'Chicago' }

];
if (groupBy = 'city'){
    groupBy.shift(2);
}
else if (groupBy = 'age'){
    groupBy.shift(1)
}
else 
    return groupBy; for (let i = 0; i < array.length; i++) {
         0
    }

console.log(groupBy);
